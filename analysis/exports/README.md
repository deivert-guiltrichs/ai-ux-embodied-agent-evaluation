# Exportaciones CSV

Esta carpeta almacenará los archivos CSV generados por `analysis/export_to_csv.py`.

Los archivos generados no deben subirse al repositorio si contienen datos reales de participantes.

Archivos esperados:

```txt
ueqs_human_ai_long.csv
condition_human_results.csv
session_metadata.csv